export enum PriceType {
  "SUBSCRIPTION" = "subscription",
  "ONE_OFF" = "one_off",
  "BOTH" = "both",
}

export enum CourseStatus {
  ARCHIVED = "archived",
  PUBLISHED = "published",
  DRAFT = "draft",
}
