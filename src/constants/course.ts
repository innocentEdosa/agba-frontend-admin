export enum PriceType {
  "SUBSCRIPTION" = "subscription",
  "ONE_OFF" = "one-off",
  "BOTH" = "both",
}

export enum CourseStatus {
  ARCHIVED = "archived",
  PUBLISHED = "published",
  DRAFT = "draft",
}
